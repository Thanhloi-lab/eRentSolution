@model eRentSolution.ViewModels.System.Users.UserResetPasswordRequest
@inject Microsoft.Extensions.Configuration.IConfiguration Configuration;
@{
    ViewData["Title"] = "ResetPassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <h1 class="mt-4">Xóa</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a class="nav-link" asp-action="index" asp-controller="home">Trang chủ</a></li>
        <li class="breadcrumb-item active">Xóa</li>
    </ol>
    <div class="card mb-4">
        <div class="card-header">
            <a class="btn btn-success" asp-action="Index" asp-controller="User">Về danh sách</a>
        </div>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="card-body">
            <div class="col-md-12">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                Thuộc tính
                            </th>
                            <th>
                                Mô tả
                            </th>
                        </tr>
                    </thead>

                    <tbody>

                        <tr>
                            <td>
                                @Html.DisplayNameFor(model => model.User.FirstName)
                            </td>
                            <td>
                                @Html.DisplayFor(model => model.User.FirstName)
                            </td>
                        </tr>
                    </tbody>
                    <tbody>

                        <tr>
                            <td>
                                @Html.DisplayNameFor(model => model.User.LastName)
                            </td>
                            <td>
                                @Html.DisplayFor(model => model.User.LastName)
                            </td>
                        </tr>
                    </tbody>
                    <tbody>

                        <tr>
                            <td>
                                @Html.DisplayNameFor(model => model.User.Dob)
                            </td>
                            <td>
                                @Html.DisplayFor(model => model.User.Dob)
                            </td>
                        </tr>
                    </tbody>
                    <tbody>

                        <tr>
                            <td>
                                @Html.DisplayNameFor(model => model.User.AvatarFilePath)

                            </td>
                            <td>
                                <a href="/User/EditAvatar/@Model.Id"><img src="@Configuration["BaseAddress"]/user-content/@Model.User.AvatarFilePath" alt="N/A" style="width: 100px; height: 100px; object-fit: cover;"></a>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <p>Mật khẩu sau khi reset sẽ là : <b style="color:red; font-size:20px" >123456aS`</b>. Vui lòng đổi mật khẩu sau khi reset.</p>
                <form asp-action="ResetPassword">
                    <input asp-for="Id" type="hidden" input />
                    <input asp-for="NewPassword" type="hidden" input />
                    <input type="submit" value="Reset" class="btn btn-danger" />
                </form>
            </div>
        </div>
    </div>
</div>

